<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBlue</item>
        <item name="colorPrimaryDark">@color/colorWhite</item>
        <item name="colorAccent">@color/colorRed</item>
    </style>

    <style name="SwitchThemeMedizin" parent="Theme.AppCompat.Light">
        <item name="android:colorControlActivated">@color/colorBlue</item>
        <item name="android:tint">@color/colorRed</item>
        <item name="android:colorButtonNormal">@color/colorBlue</item>
    </style>

    <style name="spinnerTheme">
        <item name="android:textColor">@color/colorBlack</item>
        <item name="android:background">@color/colorWhite</item>
    </style>

</resources>
